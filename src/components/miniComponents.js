import React from 'react';

export const loadingElement = (value) => {
	return <div style={{ height: value }} />;
};

export const containerElement = (value) => {
	return <div style={{ height: value }} />;
};

export const mapElement = (value) => {
	return <div style={{ height: value }} />;
};
